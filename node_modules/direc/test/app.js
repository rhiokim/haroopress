var direc = require('../');

var result = direc.find('../');
result.forEach(function(item) {
    console.log(item.mtime, item._file);
});

result = direc.sort('../', 'mtime', 'desc');
result.forEach(function(item) {
    console.log(item.mtime, item._file);
});


