#!/usr/bin/env node

var spawn = require('child_process').spawn;

switch(process.platform) {
    case 'darwin' :
        spawn('open', ['http://localhost:8000']);
    break;
    case 'win32' :
    break;
}

